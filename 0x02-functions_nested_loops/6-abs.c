#include "main.h"

/**
 * _abs - computing the absolute value of an integer
 * @n: the integer to find absolute value
 *
 * Return: gives the absolute value based on the condition
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
