#include "main.h"

/**
 * print_diagonal - draws a diagonal line on the terminal
 * @n: number of times to draw
 *
 * draws \ on the diagonal n times
 * Return: no value.
 */
void print_diagonal(int n)
{
	int i, j;

	for (i = 0 ; n > 0 ; i++)
	{
		for (j = i ; j >
}
